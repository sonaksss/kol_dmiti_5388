#ifndef _TRANS_Z_N_H_
#define _TRANS_Z_N_H_

#include "numbZ.h"
#include "../N/numbN.h"

/*
  TRANS_Z_N

  Преобразование целого неотрицательного числа в натуральное.

  Параметры:
    1) NUMBZ* z - указатель на целое число (должно быть неотрицательным)

  Возвращает указатель на NUMBN — натуральное число,
  или NULL при ошибке (z == NULL, отрицательное число,
  пустой массив цифр, ошибка выделения памяти).
*/
NUMBN* TRANS_Z_N(NUMBZ* z);

#endif