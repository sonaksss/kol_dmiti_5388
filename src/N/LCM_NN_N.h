#ifndef _LCM_NN_N_H_
#define _LCM_NN_N_H_

#include "numbN.h"

/*
  LCM_NN_N (N-14)

  Вычисление наименьшего общего кратного двух натуральных чисел.

  Параметры:
    1) NUMBN a — первое натуральное число
    2) NUMBN b — второе натуральное число

  Возвращает NUMBN — НОК(a, b).

  ОШИБКА: при невалидных входных данных или сбое зависимых модулей
  возвращается { n = -1, A = NULL }.
  Вызывающий код ОБЯЗАН проверить результат:
    if (result.n < 0 || result.A == NULL) { // ошибка }
*/
NUMBN LCM_NN_N(NUMBN a, NUMBN b);

#endif